#ifndef MAIN_H_
#define MAIN_H_

#include <stdio.h>
#include <string.h>
#include <time.h>
#include <sys/time.h>
#include <unistd.h>
#include <signal.h>
#include <stdbool.h>

enum constants {
    SUCCESS = 0,
    FPS = 30,
    SECOND = 1000000000,
    TOTAL_FRAMES = 6569,
    LENGTH = 24,
    WIDTH = 80
};

#endif /* MAIN_H_ */
